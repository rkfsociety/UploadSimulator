# Godot 4 — только локальный кэш (после clone создаётся сам)
.godot/

# Локальные секреты экспорта
export.cfg
export_credentials.cfg
*.translation

# Сборки
build/
*.apk
*.aab
*.exe
*.pck
*.zip

# IDE / агенты (локальные правила Cursor)
.cursor/

# Python — кэш (исходники tools/ в репозитории)
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/

# ОС
.DS_Store
Thumbs.db
